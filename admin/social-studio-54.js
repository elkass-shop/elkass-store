const $=id=>document.getElementById(id);let uploaded='';
const brands={elkass:{name:'ELKASS OLESNO',color:'#e30613',accent:'#7abf2e',phone:'34 358 24 42',addr:'Armii Krajowej 5, Olesno',web:'elkass-store.vercel.app'},agd:{name:'AGD OLESNO',color:'#d90416',accent:'#111827',phone:'34 358 24 42',addr:'Olesno',web:'agdolesno.pl'}};
function esc(s){return String(s||'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]||m))}
function feats(){return ($('features')?.value||'').split('\n').map(x=>x.trim()).filter(Boolean).slice(0,6)}
function data(){return {b:brands[$('brand')?.value]||brands.elkass,format:$('format')?.value||'post',theme:$('theme')?.value||'Hit tygodnia',name:$('name')?.value||'Produkt',price:$('price')?.value||'',cat:$('cat')?.value||'RTV/AGD',img:uploaded||($('img')?.value||'../assets/products/product-01-lodowka-tcl.jpg'),features:feats()}}
function dims(f){return f==='story'?[1080,1920]:f==='news'?[1400,820]:f==='reels'?[1400,780]:[1080,1080]}
function mood(t,b){if(t.includes('Black'))return ['#050505','#161616','#ffcc00'];if(t.includes('Cyber'))return ['#07111f','#0f3b57','#00e5ff'];if(t.includes('Boże')||t.includes('Mikołaj'))return ['#07111f','#153b64','#e30613'];if(t.includes('Wielkanoc')||t.includes('Wiosna'))return ['#fff7ed','#ffffff','#7abf2e'];if(t.includes('Lato'))return ['#e0f2fe','#ffffff','#facc15'];if(t.includes('Jesień'))return ['#fff7ed','#ffffff','#f97316'];if(t.includes('Zima'))return ['#e0f2fe','#ffffff','#38bdf8'];return ['#ffffff','#f6f8fb',b.color]}
function svg(format){const d=data();format=format||d.format;const [w,h]=dims(format);const [c1,c2,accent]=mood(d.theme,d.b);const dark=c1==='#050505'||c1==='#07111f';const txt=dark?'#fff':'#07111f';const fs=d.features.length?d.features:['Dostępny w sklepie','Doradztwo na miejscu','Promocyjna oferta'];if(format==='reels'){return `<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}"><rect width="${w}" height="${h}" fill="#eef2f7"/><text x="40" y="70" font-size="40" font-family="Arial Black,Arial" fill="${d.b.color}">Storyboard rolki — ${esc(d.theme)}</text><text x="40" y="135" font-size="28" font-family="Arial" fill="#111">Logo → produkt → cechy → cena → kontakt</text><image href="${esc(d.img)}" x="520" y="190" width="360" height="420" preserveAspectRatio="xMidYMid meet"/><text x="40" y="260" font-size="48" font-family="Arial Black,Arial" fill="#111">${esc(d.name)}</text><text x="40" y="340" font-size="76" font-family="Arial Black,Arial" fill="${d.b.color}">${esc(d.price)}</text>${fs.map((f,i)=>`<text x="40" y="${420+i*42}" font-size="28" font-family="Arial" fill="#111">✓ ${esc(f)}</text>`).join('')}<rect x="0" y="${h-80}" width="${w}" height="80" fill="${d.b.color}"/><text x="40" y="${h-30}" font-size="30" font-family="Arial" font-weight="900" fill="#fff">${esc(d.b.name)} • ${esc(d.b.phone)}</text></svg>`}
return `<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}"><defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop stop-color="${c1}"/><stop offset="1" stop-color="${c2}"/></linearGradient><filter id="shadow"><feDropShadow dx="0" dy="22" stdDeviation="18" flood-opacity=".35"/></filter></defs><rect width="${w}" height="${h}" fill="url(#g)"/><circle cx="${w*.86}" cy="${h*.13}" r="${w*.18}" fill="white" opacity=".14"/><rect x="${w*.055}" y="${h*.055}" width="${w*.40}" height="${h*.105}" rx="28" fill="white"/><text x="${w*.085}" y="${h*.125}" font-size="${format==='story'?56:44}" font-family="Arial Black,Arial" fill="${d.b.color}">${esc(d.b.name)}</text><text x="${w*.08}" y="${h*.29}" font-size="${format==='story'?82:64}" font-family="Arial Black,Arial" fill="${txt}">${esc(d.theme)}</text><text x="${w*.08}" y="${h*.41}" font-size="${format==='story'?48:44}" font-family="Arial Black,Arial" fill="${txt}">${esc(d.name)}</text>${fs.map((f,i)=>`<text x="${w*.08}" y="${h*.49+i*(format==='story'?58:44)}" font-size="${format==='story'?34:28}" font-family="Arial" font-weight="700" fill="${txt}">✓ ${esc(f)}</text>`).join('')}<image href="${esc(d.img)}" x="${w*.56}" y="${h*.30}" width="${w*.40}" height="${h*.43}" preserveAspectRatio="xMidYMid meet" filter="url(#shadow)"/><rect x="${w*.08}" y="${h*.76}" width="${w*.43}" height="${h*.12}" rx="30" fill="white"/><text x="${w*.11}" y="${h*.84}" font-size="${format==='story'?92:76}" font-family="Arial Black,Arial" fill="${d.b.color}">${esc(d.price)}</text><rect x="0" y="${h-76}" width="${w}" height="76" fill="${d.b.color}"/><text x="${w*.08}" y="${h-28}" font-size="30" font-family="Arial" font-weight="900" fill="white">${esc(d.b.phone)} • ${esc(d.b.addr)} • ${esc(d.b.web)}</text></svg>`}
function postText(){const d=data();return `${d.theme} w ${d.b.name}\n\n${d.name}\n${d.features.map(x=>'✅ '+x).join('\n')}\n\nCena: ${d.price}\n☎ ${d.b.phone}\n📍 ${d.b.addr}\n🌐 ${d.b.web}\n\n#ELKASS #RTV #AGD #Olesno #Promocja`}
function render(){const p=$('preview'), t=$('postText'); if(p)p.innerHTML=svg(); if(t)t.value=postText(); document.querySelectorAll('nav button').forEach(b=>b.classList.toggle('active',b.dataset.f===($('format')?.value||'post')))}
function dl(name,content,type){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([content],{type}));a.download=name;a.click()}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('input,select,textarea').forEach(x=>x.addEventListener('input',render));document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{if($('format'))$('format').value=b.dataset.f;render()});const f=$('file'); if(f)f.onchange=e=>{const file=e.target.files[0]; if(!file)return; const r=new FileReader(); r.onload=ev=>{uploaded=ev.target.result;render()}; r.readAsDataURL(file)}; const dc=$('downloadCurrent'); if(dc)dc.onclick=()=>dl('ELKASS_'+($('format')?.value||'post')+'.svg',svg(),'image/svg+xml'); const dp=$('downloadPack'); if(dp)dp.onclick=()=>{['post','story','reels','news'].forEach(f=>dl('ELKASS_'+f+'.svg',svg(f),'image/svg+xml'));dl('tresc_posta.txt',postText(),'text/plain')}; const ct=$('copyText'); if(ct)ct.onclick=()=>{navigator.clipboard&&navigator.clipboard.writeText(postText());alert('Skopiowano treść posta')}; render();});
